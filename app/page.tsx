// Phase 2 will build the full home page.
// This stub keeps the build passing for Phase 1.

export default function Home() {
  return (
    <section style={{ padding: '4rem var(--outer-margin)' }}>
      <p style={{ fontFamily: 'var(--font-sans)', color: 'var(--paper-dim)' }}>
        Home — Phase 2
      </p>
    </section>
  )
}
